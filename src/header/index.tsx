import * as React from "react";

import * as styles from "./index.css";

interface Props {}

const Header = (props: Props) => (
  <header className={styles.header}>I'm a header!</header>
);

export default Header;
